# 日志(不能上传)
*.log
nohup.out

# SQLite 数据库及 WAL 附属文件(运行数据,本地保留)
backend/board.db
backend/board.db-shm
backend/board.db-wal

# 图标缓存(后台线程自动重新下载)
backend/icons/

# Python
__pycache__/
*.pyc

# 运行时
.server.pid
*.pid

# 本地秘密配置
tg/tg.json
tg/tg.json.disabled
.env
